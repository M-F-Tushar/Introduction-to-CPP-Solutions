#include <iostream>
using std::cout;

int main()
{
//      cout << "THis is "
        cout << "the first"; /* A program with multiple outputs
        cout << " trap"; /* That’s the last one. */
        return 0;
}